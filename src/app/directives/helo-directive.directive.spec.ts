import { HeloDirectiveDirective } from './helo-directive.directive';

describe('HeloDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HeloDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
